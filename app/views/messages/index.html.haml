!!!
%html
  %head
    %meta{charset: "utf-8"}
    %title  Chatspace
  %body
    #wrapper
      .sidebar
        .user-info
          = render "user-info"
        .groups
          .group
            .group__name
              = render "group__name"
            .group__message
              = render "group__message"
          .group
            .group__name
              = render "group__name"
            .group__message
              = render "group__message"
          .group
            .group__name
              = render "group__name"
            .group__message
              = render "group__message"

      .chatpage
        .group
        .message

        .inputbox
